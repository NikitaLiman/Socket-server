{
  "compilerOptions": {
    "target": "ES6",
    "module": "CommonJS",
    "outDir": "./dist", // Убедитесь, что файлы компилируются сюда
    "rootDir": "./src", // Убедитесь, что исходные файлы находятся в папке src
    "strict": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules"]
}
